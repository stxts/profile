<!DOCTYPE html>
<html>
  <!-- Header -->
  <%= render 'layouts/header' %>

  <!-- Navbar -->
  <%= render 'layouts/navbar' %> 
  
  <body>
    <%= yield %>
  </body>

  <!-- Footer -->
  <%= render 'layouts/footer' %> 
</html>
